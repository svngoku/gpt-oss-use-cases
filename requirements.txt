# Core dependencies
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
tokenizers>=0.15.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0
pydantic>=2.0.0
python-dotenv>=1.0.0
rich>=13.0.0
typer>=0.9.0
pyyaml>=6.0

# ML tracking and model hub
wandb>=0.16.0
huggingface-hub>=0.19.0

# GPU optimization
triton>=2.1.0

# Provider dependencies
requests>=2.31.0
aiohttp>=3.8.0

# Optional provider dependencies (install as needed)
# For Ollama: no additional dependencies
# For vLLM: pip install vllm
# For OpenRouter: API key required
# For LangChain: pip install langchain langchain-community
